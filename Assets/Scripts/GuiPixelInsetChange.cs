using UnityEngine;
using System.Collections;

public class GuiPixelInsetChange : MonoBehaviour {

	Vector2 pixelInsetVal;


	void Start()
	{
		pixelInsetVal.x = Screen.width / 2f;
		pixelInsetVal.y = Screen.height /2f;
		}
	
	// Update is called once per frame
	void Update () {
	
	}
}
